
@{
    ViewBag.Title = "_Actors";
    Layout = "~/Views/Shared/_MainLayout.cshtml";
}
@using ProjectTemplate.Core.Entities;
@model List<Actor>

@section Body{
    <div class="right-content">
        <div class="right-content-heading">
            <div class="right-content-heading-left">
                <h3 class="head">Oyuncular</h3>
            </div>
        </div>
        <div class="content-grids">
            @foreach (Actor item in Model)
            {
                <div class="content-grid">
                    <a class="play-icon" href="@Url.Action("ActorDetail", "Actor")/@item.Id"><img src="@item.ImagePath" title="@item.Name" /></a>
                    <h3>@item.Name @item.Surname</h3>
                    <a class="button play-icon popup-with-zoom-anim" href="#small-dialog">Detay</a>
                </div>
            }
            <div class="clearfix"> </div>
        </div>
    </div>
}