
@{
    ViewBag.Title = "_Directors";
    Layout = "~/Views/Shared/_MainLayout.cshtml";
}
@using ProjectTemplate.Core.Entities;
@model List<Director>

@section Body{
    <div class="right-content">
        <div class="right-content-heading">
            <div class="right-content-heading-left">
                <h3 class="head">Yönetmenler</h3>
            </div>
        </div>
        <div class="content-grids">
            @foreach (Director item in Model)
            {
                <div class="content-grid">
                    <a class="play-icon" href="@Url.Action("DirectorDetail", "Director")/@item.Id"><img src="@item.ImagePath" title="@item.Name" /></a>
                    <h3>@item.Name @item.Surname</h3>
                    <a class="button play-icon popup-with-zoom-anim" href="#small-dialog">Detay</a>
                </div>
            }
            <div class="clearfix"> </div>
        </div>
    </div>
}